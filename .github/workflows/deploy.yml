name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4 # Note: Use the latest version

      - name: Set up Node.js
        uses: actions/setup-node@v4 # Note: Use the latest version
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build with environment variables
        run: npm run build
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}

      - name: Setup Pages
        uses: actions/configure-pages@v4 # Note: Use the latest version

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3 # Note: Use the latest version
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # Note: Use the latest version
